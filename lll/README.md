# lll

## Execute  

* Run `stack exec -- lll-exe` to see "We're inside the application!"
* With `stack exec -- lll-exe --verbose` you will see the same message, with more logging.

## Run tests

`stack test`
